import { ShowOnMouseOverDirective } from './show-on-mouse-over.directive';

describe('ShowOnMouseOverDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowOnMouseOverDirective();
    expect(directive).toBeTruthy();
  });
});
